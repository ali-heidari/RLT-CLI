# config.toml
debug = false

interval_secs = 10
batch_size=128
total_batches=20000
input_number=6
output_number=3
hidden_layers=16
reply_capacity=4096
dataset = "./data/train.csv"
model_name="model-128.json"
reward_script="./scripts/reward_script.py"
log_interval=64
mode="Infer"
# Optional compute backend: "Cpu" (default) or "Gpu".
# The --backend CLI flag takes precedence over this value.
# backend="Gpu"
